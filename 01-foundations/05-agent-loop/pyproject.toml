[project]
name = "lesson-agent-loop"
version = "0.1.0"
description = "Lesson 5: Agent Loop - Autonomous agents that use tools iteratively"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "common",
    "openai>=1.0.0",
    "openrouter>=1.1.74",
    "python-dotenv>=1.0.0",
    "rich>=14.0.0",
]

[tool.uv.sources]
common = { path = "../../common", editable = true }

[tool.ruff]
line-length = 100
target-version = "py311"
