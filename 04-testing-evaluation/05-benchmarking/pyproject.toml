[project]
name = "lesson-benchmarking"
version = "0.1.0"
description = "Lesson: Benchmarking - Model, prompt, and architecture comparison"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "common",
    "janome>=0.5.0",  # 日本語の分かち書き（検索クエリのトークナイズ）
    "openai>=1.0.0",
    "openrouter>=1.1.74",  # OpenRouter API client
    "python-dotenv>=1.0.0",
    "rich>=13.7.0",
]

[tool.uv.sources]
common = { path = "../../common", editable = true }

[tool.ruff]
line-length = 100
target-version = "py311"
