[project]
name = "lesson-tracing-debugging"
version = "0.1.0"
description = "Lesson: Tracing & Debugging - Observability, execution traces, failure analysis"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "common",
    "janome>=0.5.0",  # 日本語の分かち書き（検索クエリのトークナイズ）
    "openrouter>=1.1.74",  # OpenRouter API client
    "python-dotenv>=1.0.0",
    "rich>=13.7.0",
]

[tool.uv.sources]
common = { path = "../../common", editable = true }

[tool.ruff]
line-length = 100
target-version = "py311"
