[project]
name = "lesson-red-teaming-safety"
version = "0.1.0"
description = "Lesson: Red Teaming & Safety - Adversarial testing, prompt injection, guardrails"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "common",
    "openrouter>=1.1.74",  # OpenRouter API client
    "python-dotenv>=1.0.0",
    "rich>=13.7.0",
]

[tool.uv.sources]
common = { path = "../../common", editable = true }

[tool.ruff]
line-length = 100
target-version = "py311"
